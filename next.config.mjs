/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // basePath: '/jecim',
    // assetPrefix: 'https://inforisdik.jec.co.id/jecim/',
};

export default nextConfig;
